let num = 0;
let counter = 0;
let string = '';
const a = (min, max) => Math.floor(Math.random() * (max - min + 1)) + min;
const alphabet = 'abcdefghijklmnopqrstuvwxyz';

let c1 = 0;
let c2 = 0;
let c3 = 0;
let length = 0;
while (string !== "f") {
  string = alphabet[c1];
  c1++;
  console.log(string)
}
// dpeends on boolean or length
